// ************
// Types
// ************

import '@dev.hiconic/gm_root-model';
import '@dev.hiconic/gm_gm-core-api';

import { T } from '@dev.hiconic/hc-js-base';

export declare namespace meta {
	const groupId: string;
	const artifactId: string;
	const version: string;
}

export import HasPagination = T.com.braintribe.model.pagination.HasPagination;
export import Paginated = T.com.braintribe.model.pagination.Paginated;

declare module '@dev.hiconic/hc-js-base' {

	namespace T.com.braintribe.model.pagination {

		const HasPagination: hc.reflection.EntityType<HasPagination>;
		type HasPagination = T.com.braintribe.model.generic.GenericEntity &
		  Entity<"com.braintribe.model.pagination.HasPagination", {
			pageLimit: P<integer, { nullable: false }>;
			pageOffset: P<integer, { nullable: false }>;
		}>;

		const Paginated: hc.reflection.EntityType<Paginated>;
		type Paginated = T.com.braintribe.model.generic.GenericEntity &
		  Entity<"com.braintribe.model.pagination.Paginated", {
			hasMore: P<boolean, { nullable: false }>;
		}>;

	}

}
