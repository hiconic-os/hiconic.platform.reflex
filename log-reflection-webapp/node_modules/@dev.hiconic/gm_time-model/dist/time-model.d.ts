// ************
// Types
// ************

import '@dev.hiconic/gm_root-model';
import '@dev.hiconic/gm_gm-core-api';

import { T } from '@dev.hiconic/hc-js-base';

export declare namespace meta {
	const groupId: string;
	const artifactId: string;
	const version: string;
}

export import CalendarOffset = T.com.braintribe.model.time.CalendarOffset;
export import DateOffset = T.com.braintribe.model.time.DateOffset;
export import DateOffsetUnit = T.com.braintribe.model.time.DateOffsetUnit;
export import TimeSpan = T.com.braintribe.model.time.TimeSpan;
export import TimeUnit = T.com.braintribe.model.time.TimeUnit;
export import TimeZoneOffset = T.com.braintribe.model.time.TimeZoneOffset;

declare module '@dev.hiconic/hc-js-base' {

	namespace T.com.braintribe.model.time {

		const CalendarOffset: hc.reflection.EntityType<CalendarOffset>;
		type CalendarOffset = T.com.braintribe.model.generic.GenericEntity &
		  Entity<"com.braintribe.model.time.CalendarOffset">;

		const DateOffset: hc.reflection.EntityType<DateOffset>;
		type DateOffset = CalendarOffset &
		  Entity<"com.braintribe.model.time.DateOffset", {
			offset: DateOffsetUnit;
			value: P<integer, { nullable: false }>;
		}>;

		interface DateOffsetUnit extends hc.reflection.EnumBase<DateOffsetUnit>, hc.Enum<DateOffsetUnit> {}
		const DateOffsetUnit: {
			readonly [hc.Symbol.enumType]: hc.reflection.EnumType<DateOffsetUnit>,
			readonly millisecond: DateOffsetUnit,
			readonly second: DateOffsetUnit,
			readonly minute: DateOffsetUnit,
			readonly hour: DateOffsetUnit,
			readonly day: DateOffsetUnit,
			readonly month: DateOffsetUnit,
			readonly year: DateOffsetUnit,
		}

		const TimeSpan: hc.reflection.EntityType<TimeSpan>;
		type TimeSpan = T.com.braintribe.model.generic.GenericEntity &
		  Entity<"com.braintribe.model.time.TimeSpan", {
			unit: TimeUnit;
			value: P<double, { nullable: false }>;
		}>;

		interface TimeUnit extends hc.reflection.EnumBase<TimeUnit>, hc.Enum<TimeUnit> {}
		const TimeUnit: {
			readonly [hc.Symbol.enumType]: hc.reflection.EnumType<TimeUnit>,
			readonly planckTime: TimeUnit,
			readonly nanoSecond: TimeUnit,
			readonly microSecond: TimeUnit,
			readonly milliSecond: TimeUnit,
			readonly second: TimeUnit,
			readonly minute: TimeUnit,
			readonly hour: TimeUnit,
			readonly day: TimeUnit,
		}

		const TimeZoneOffset: hc.reflection.EntityType<TimeZoneOffset>;
		type TimeZoneOffset = CalendarOffset &
		  Entity<"com.braintribe.model.time.TimeZoneOffset", {
			minutes: P<integer, { nullable: false }>;
		}>;

	}

}
