// ************
// Types
// ************

import '@dev.hiconic/gm_root-model';
import '@dev.hiconic/gm_user-model';
import '@dev.hiconic/gm_time-model';
import '@dev.hiconic/gm_gm-core-api';

import { T } from '@dev.hiconic/hc-js-base';

export declare namespace meta {
	const groupId: string;
	const artifactId: string;
	const version: string;
}

export import UserSession = T.com.braintribe.model.usersession.UserSession;
export import UserSessionType = T.com.braintribe.model.usersession.UserSessionType;

declare module '@dev.hiconic/hc-js-base' {

	namespace T.com.braintribe.model.usersession {

		const UserSession: hc.reflection.EntityType<UserSession>;
		type UserSession = T.com.braintribe.model.generic.GenericEntity &
		  Entity<"com.braintribe.model.usersession.UserSession", {
			accessId: string;
			creationDate: date;
			creationInternetAddress: string;
			creationNodeId: string;
			effectiveRoles: set<string>;
			expiryDate: date;
			fixedExpiryDate: date;
			isInvalidated: P<boolean, { nullable: false }>;
			lastAccessedDate: date;
			maxIdleTime: T.com.braintribe.model.time.TimeSpan;
			properties: map<string, string>;
			referenceCounter: P<integer, { nullable: false }>;
			sessionId: string;
			type: UserSessionType;
			user: T.com.braintribe.model.user.User;
		}>;

		interface UserSessionType extends hc.reflection.EnumBase<UserSessionType>, hc.Enum<UserSessionType> {}
		const UserSessionType: {
			readonly [hc.Symbol.enumType]: hc.reflection.EnumType<UserSessionType>,
			readonly trusted: UserSessionType,
			readonly normal: UserSessionType,
			readonly internal: UserSessionType,
		}

	}

}
