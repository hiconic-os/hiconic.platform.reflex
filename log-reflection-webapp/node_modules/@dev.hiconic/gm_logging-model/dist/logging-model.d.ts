// ************
// Types
// ************

import '@dev.hiconic/gm_root-model';
import '@dev.hiconic/gm_gm-core-api';

import { T } from '@dev.hiconic/hc-js-base';

export declare namespace meta {
	const groupId: string;
	const artifactId: string;
	const version: string;
}

export import LogLevel = T.com.braintribe.model.logging.LogLevel;

declare module '@dev.hiconic/hc-js-base' {

	namespace T.com.braintribe.model.logging {

		interface LogLevel extends hc.reflection.EnumBase<LogLevel>, hc.Enum<LogLevel> {}
		const LogLevel: {
			readonly [hc.Symbol.enumType]: hc.reflection.EnumType<LogLevel>,
			readonly TRACE: LogLevel,
			readonly DEBUG: LogLevel,
			readonly INFO: LogLevel,
			readonly WARN: LogLevel,
			readonly ERROR: LogLevel,
		}

	}

}
