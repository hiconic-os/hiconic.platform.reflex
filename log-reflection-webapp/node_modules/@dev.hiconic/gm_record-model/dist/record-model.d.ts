// ************
// Types
// ************

import '@dev.hiconic/gm_root-model';
import '@dev.hiconic/gm_gm-core-api';

import { T } from '@dev.hiconic/hc-js-base';

export declare namespace meta {
	const groupId: string;
	const artifactId: string;
	const version: string;
}

export import ListRecord = T.com.braintribe.model.record.ListRecord;
export import MapRecord = T.com.braintribe.model.record.MapRecord;
export import Record = T.com.braintribe.model.record.Record;

declare module '@dev.hiconic/hc-js-base' {

	namespace T.com.braintribe.model.record {

		const ListRecord: hc.reflection.EntityType<ListRecord>;
		type ListRecord = Record &
		  Entity<"com.braintribe.model.record.ListRecord", {
			values: list<CollectionElement>;
		}>;

		const MapRecord: hc.reflection.EntityType<MapRecord>;
		type MapRecord = Record &
		  Entity<"com.braintribe.model.record.MapRecord", {
			values: map<string, CollectionElement>;
		}>;

		const Record: hc.reflection.EntityType<Record>;
		type Record = T.com.braintribe.model.generic.GenericEntity &
		  Entity<"com.braintribe.model.record.Record">;

	}

}
